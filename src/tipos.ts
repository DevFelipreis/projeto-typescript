export type Carro = {
    id: number | string,
    marca: string,
    modelo: string,
    ano: number,
    cor: string,
    valor: number
};

export type Marca = {
    id: number | string,
    marca: string
};